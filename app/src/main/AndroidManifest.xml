<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.matemate">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-sdk android:minSdkVersion="26" />
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home.MainActivity">
        </activity>
        <activity android:name=".Join.JoinActivity"/>
        <activity android:name=".WritePost.EditFragment"/>
        <activity android:name=".Participate.ParticipateActivity"/>
        <meta-data
            android:name="server_url"
            android:value="app.BuildConfig.server_url">
        </meta-data>
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="app.BuildConfig.api_key">
        </meta-data>
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="app.BuildConfig.app_key">
        </meta-data>
    </application>

</manifest>